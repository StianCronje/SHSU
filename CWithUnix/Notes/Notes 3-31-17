Notes 3/29/17
Special Programming
Burris

Pipes and Sockets cont. (pg 7)

execlp( "ls", "ls", "-1", NULL );  //Execute “ls” command writing to pipe.
                   // execlp(<pgm name>, <pgm name including path>, <parmlist>);
                   //Terminate parm list with “NULL.”

